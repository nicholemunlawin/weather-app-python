### **Simple Weather App project**



Takes user input from the search bar with location or place name and process it through geolocation API for latitude and longitude acquiring which will then be processed through the weather API which takes latitude and longitude as reference values. This app will then show the "Temperature and Wind speed" values from the app itself.

